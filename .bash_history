
./gradlew cleanEclipse eclipse && echo -n "cd /opt/platform2 " && cd /opt/platform2/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/urjacommons " && cd /opt/urjacommons/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/platform2 " && cd /opt/platform2/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse
#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/urjacommons " && cd /opt/urjacommons/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/platform2 " && cd /opt/platform2/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse
#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/urjacommons " && cd /opt/urjacommons/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/urjacommons " && cd /opt/urjacommons/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/platform2 " && cd /opt/platform2/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/urjacommons " && cd /opt/urjacommons/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/platform2 " && cd /opt/platform2/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse
#!/bin/bash
# Purpose:
#       * bootstrap machine in order to prepare for environment setup
#Author: Naveen Chakaravarthy
#Date/Time: 28-06-2017.11:10
#Modified date: Date with Reason for modification
set -e
echo -n "Enter Your BitBucket Password > "
read bitbucketpass
echo -n "cd /opt/templates "
cd /opt/templates/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/extractiondomain " && cd /opt/extractiondomain/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/templatesdk " && cd /opt/templatesdk/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/auditoverrides1 " && cd /opt/auditoverrides1/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/urjacommons " && cd /opt/urjacommons/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse && echo -n "cd /opt/platform2 " && cd /opt/platform2/ && git checkout . && 
/usr/bin/expect - << EOF
	spawn git pull
	expect "Password*"
	send "$bitbucketpass\r"
	set timeout 5000
	expect eof
EOF

./gradlew cleanEclipse eclipse
